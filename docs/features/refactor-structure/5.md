# Task 5: Move home-info-repository to src/db/ and update imports

## Description

Move the `home-info-repository.ts` from `src/repositories/` to `src/db/`. The `IHomeInfoRepository` interface remains in `src/usecases/get-home-info-use-case.ts` since home-info is not a domain entity.

## Actions

1. Move `src/repositories/home-info-repository.ts` to `src/db/home-info-repository.ts`
2. Update the import in `src/db/home-info-repository.ts` from `../usecases/get-home-info-use-case.js` to use the local interface from the same file (since `IHomeInfoRepository` stays in the use case)
3. Update the import in `src/routes/home-info.route.ts` to import `HomeInfoRepository` from `../db/home-info-repository.js`

## Files to Update

- `src/repositories/home-info-repository.ts` → `src/db/home-info-repository.ts`
- `src/routes/home-info.route.ts` - Update import path
- `src/db/home-info-repository.ts` - Update internal import for `IHomeInfoRepository`

## Note

Since `IHomeInfoRepository` stays in `src/usecases/get-home-info-use-case.ts`, the repository implementation in `src/db/home-info-repository.ts` will need to import it from there.

## Acceptance Criteria

- [ ] `src/db/home-info-repository.ts` exists
- [ ] `src/repositories/home-info-repository.ts` does not exist
- [ ] `HomeInfoRepository` is imported from `../db/home-info-repository.js` in routes
- [ ] TypeScript compilation succeeds
