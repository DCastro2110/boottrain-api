# Task 1: Rename domain file: workou-day.ts → workout-day.ts

## Description

Fix the typo in the domain file name by renaming `workou-day.ts` to `workout-day.ts`.

## Actions

1. Rename `src/domain/workou-day.ts` to `src/domain/workout-day.ts`
2. Update the file content to use the correct import path if any self-references exist
3. Update any files that import from `workou-day.ts` to use `workout-day.ts`

## Files Affected

- `src/domain/workou-day.ts` → `src/domain/workout-day.ts`

## Import Updates Required

- Check all files that import from `../domain/workou-day` or `./workou-day` and update to `../domain/workout-day` or `./workout-day`

## Acceptance Criteria

- [ ] File `src/domain/workout-day.ts` exists
- [ ] File `src/domain/workou-day.ts` does not exist
- [ ] All imports referencing `workou-day` are updated to `workout-day`
- [ ] TypeScript compilation succeeds
