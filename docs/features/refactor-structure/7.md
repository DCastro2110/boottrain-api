# Task 7: Update all imports and remove src/repositories/ folder

## Description

Update all remaining imports across the codebase to reflect the new structure, then remove the empty `src/repositories/` folder.

## Actions

1. Update imports in route files:
   - `src/routes/workout-plan.route.ts`: Update imports for `WorkoutPlanRepository` and `WorkoutSessionRepository` from `../repositories/` to `../db/`
   - `src/routes/home-info.route.ts`: Update import for `HomeInfoRepository` from `../repositories/` to `../db/`

2. Verify all imports are correct throughout the codebase

3. Remove the empty `src/repositories/` directory

4. Run TypeScript compilation and ESLint to verify no broken imports

## Files to Update

- `src/routes/workout-plan.route.ts`
- `src/routes/home-info.route.ts`

## Final Verification

- [ ] All imports referencing `../repositories/` are updated to `../db/`
- [ ] `src/repositories/` folder does not exist
- [ ] TypeScript compilation succeeds (`pnpm tsc --noEmit`)
- [ ] ESLint passes (`pnpm eslint .`)
- [ ] Application still starts correctly (`pnpm dev`)
