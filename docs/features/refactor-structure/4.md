# Task 4: Create IWorkoutSessionRepository interface in domain/workout-session.ts

## Description

Move the `IWorkoutSessionRepository` interface from `src/usecases/start-session-use-case.ts` to `src/domain/workout-session.ts`.

## Actions

1. Read the current `IWorkoutSessionRepository` interface from `src/usecases/start-session-use-case.ts`
2. Copy the interface to `src/domain/workout-session.ts`
3. Update the imports in `src/domain/workout-session.ts` to include any needed types (e.g., `IWorkoutSession`)
4. Remove the interface from `src/usecases/start-session-use-case.ts`
5. Update the imports in `src/usecases/start-session-use-case.ts` to use the interface from `../domain/workout-session.js`
6. Update any other files that import `IWorkoutSessionRepository` from `start-session-use-case.ts` to import from `../domain/workout-session.js`

## Files to Update

- `src/domain/workout-session.ts` - Add interface
- `src/usecases/start-session-use-case.ts` - Remove interface, add import
- `src/repositories/workout-session-repository.ts` - Update import path
- `src/usecases/get-workout-day-use-case.ts` - Update import path
- `src/usecases/close-session-use-case.ts` - Update import path

## Acceptance Criteria

- [ ] `IWorkoutSessionRepository` interface exists in `src/domain/workout-session.ts`
- [ ] `IWorkoutSessionRepository` interface is removed from `src/usecases/start-session-use-case.ts`
- [ ] All imports of `IWorkoutSessionRepository` point to `../domain/workout-session.js`
- [ ] `src/repositories/workout-session-repository.ts` imports `IWorkoutSessionRepository` from domain
- [ ] TypeScript compilation succeeds
