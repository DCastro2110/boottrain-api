# 6. Prisma schema/migration for missing fields

## Description

Add required DB fields if missing (expected: `WorkoutDay.coverImageUrl`) and create Prisma migration + client regeneration.

## Acceptance Criteria

- `prisma/schema.prisma` contains required new field(s) for Home Info payload.
- A new migration is created under `prisma/migrations/`.
- Prisma client is regenerated successfully to `generated/prisma`.
- Existing code compiles against updated generated types.
