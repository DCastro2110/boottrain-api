# 2. Add use case and repository abstraction for Home Info

## Description

Add `create-home-info-use-case.ts` with DTOs and `IHomeInfoRepository`, plus orchestration logic entrypoint (`execute`).

## Acceptance Criteria

- Use case file exists with typed `InputDTO` and `OutputDTO`.
- `IHomeInfoRepository` interface is defined in use case file (matching project pattern).
- `execute` accepts `userId` and returns the full Home Info output shape.
- No Prisma calls are made directly in route layer.
