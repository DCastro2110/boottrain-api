# Subtask 2: Create IWorkoutSessionRepository interface

## Description

Create the repository interface in `src/usecases/start-session-use-case.ts` (following existing codebase convention where interface is defined in use case file).

## Details

- File: `src/usecases/start-session-use-case.ts`
- Define `IWorkoutSessionRepository` interface alongside the use case

## Interface Shape

```typescript
export interface IWorkoutSessionRepository {
  startSession(workoutDayId: string, userId: string): Promise<{ id: string }>;
  findByWorkoutDayIdAndNotCompleted(
    workoutDayId: string,
  ): Promise<IWorkoutSession | null>;
  findById(id: string): Promise<IWorkoutSession | null>;
}
```

## Acceptance Criteria

- [ ] Interface defines `startSession(workoutDayId: string, userId: string): Promise<{ id: string }>`
- [ ] Interface includes `findByWorkoutDayIdAndNotCompleted` method to check for existing active sessions
- [ ] Interface includes `findById` method for retrieval
- [ ] Follows existing codebase convention (interface in use case file)
