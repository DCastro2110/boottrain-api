# Subtask 1: Create IWorkoutSession domain interface

## Description

Create the interface file at `src/domain/workout-session.ts` defining `IWorkoutSession`.

## Details

- File: `src/domain/workout-session.ts`
- Export the `IWorkoutSession` interface

## Interface Shape

```typescript
export interface IWorkoutSession {
  id: string;
  userId: string;
  workoutDayId: string;
  startedAt: Date;
  completedAt: Date | null;
  durationInSeconds: number;
}
```

## Acceptance Criteria

- [ ] The file exports `IWorkoutSession` interface
- [ ] Interface includes: id, userId, workoutDayId, startedAt, completedAt, durationInSeconds
- [ ] Uses proper TypeScript types (Date, not DateTime string)
