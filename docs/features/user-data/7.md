# Subtask 7: Register user routes in index.ts

## Description

Add user routes registration to `src/index.ts`.

## Details

1. Read `src/index.ts` to understand current route registration pattern
2. Import `userRoutes` from `./routes/user.route.js` (note .js extension for ESM)
3. Register user routes with prefix "/users"
4. Ensure the import path uses .js extension as per project conventions

## Acceptance Criteria

- [ ] user.route.ts is imported in index.ts
- [ ] Route is registered with prefix "/users"
- [ ] Server starts without errors
- [ ] TypeScript compilation passes